template:
  module:
    source:
      'templates/module.mstch.cpp'
  cxx_record:
    source:
      'templates/cxx_record.mstch.cpp'
  enum:
    source:
      'templates/enum.mstch.cpp'
  function:
    source:
      'templates/function.mstch.cpp'
  variable:
    source:
      'templates/variable.mstch.cpp'
  file:
    header: |
      #include "get_signature.hpp"
      #include "chimera.cpp"
  main:
    precontent: |
      boost::python::import("boost_numpy_eigen");
      boost::python::import("dartpy");
namespaces:
  'aikido':
    name: null  # Collapse the 'aikido' namespace into its parent.
types:
  # Suppressed due to bugs in Chimera:
  'std::unique_ptr<aikido::util::RNG>': null
classes:
  # Suppressed due to bugs in Chimera:
  'aikido::constraint::RnBoxConstraint': null
  'aikido::constraint::SO2Sampleable': null
  'aikido::constraint::SO3UniformSampler': null
  'aikido::constraint::TSR': null
  'aikido::planner::ompl::GoalRegion': null
  'aikido::planner::ompl::StateSampler': null
